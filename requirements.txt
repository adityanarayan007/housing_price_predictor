# --- Core ML Dependencies ---
pandas
scikit-learn
joblib
numpy # Generally needed by scikit-learn/pandas (good to include)

# --- API and Serving Dependencies (Production Runtime) ---
fastapi          # The web framework itself (The missing piece!)
uvicorn          # The high-speed ASGI server used by Gunicorn
gunicorn         # The robust WSGI server (worker manager) for production